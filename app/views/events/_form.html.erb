<div class="row">
  <div class="large-6 columns large-centered">
<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
    <%= f.label :title %>
    <%= f.text_field :title %>
  
  
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  
  
    <%= f.label :link %>
    <%= f.text_field :link %>

    <p><code>https://law-chapman-csm.symplicity.com/students/index.php?ss=ws&_ksl=1&mode=list&s=event</code></p><br>
  

    <%= f.submit :class => "small button success radius" %>
    <%= f.submit "Save & Add Another", :class => "small button success radius", :name => "add_another" %>
    <%= link_to 'Back', preview_url %>

<% end %>

